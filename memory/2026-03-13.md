
## Skill install policy
- User decided that all future skill installs must be vetted with `skill-vetter` before installation.
- Workflow: vet first, install only if the vet report looks clean; otherwise stop and ask.
